import 'package:flame/components.dart';
import 'package:flutter/material.dart';

class GameTheme {
  //game colors
  static const Color brown = Color(0xFF4a1a06);
  static const Color orange = Color(0xFFf7931e);
  static const Color blue = Color(0xFF29a0ec);

  static TextPaint regular = TextPaint(
      style: const TextStyle(
    fontSize: 36, // Adjust the font size as needed
    color: Colors.white,
    fontFamily: 'SinglyLinked',
  ));
}
