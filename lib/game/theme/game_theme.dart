import 'package:flame/components.dart';
import 'package:flutter/material.dart';

class GameTheme {
  static TextPaint regular = TextPaint(
      style: const TextStyle(
    fontSize: 36, // Adjust the font size as needed
    color: Colors.white,
    fontFamily: 'SinglyLinked',
  ));
}
